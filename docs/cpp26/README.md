面向 AI 的语言，它的编译器也需要高性能。

C++26 特性与语言核心的对应关系：

```
01 — 增量编译基座      ← Modules（编译器的模块化依赖图）
02 — 编译期代码能力    ← std::meta（反射 + splice + consteval）
03 — 编译器可验证       ← Contracts（不变量约束）
```

不是"后端代码生成"，是语言自身的编译器基础设施。
